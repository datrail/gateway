[pytest]
# Strict mode: an async test without the marker fails rather than being
# silently collected and skipped, which is how a test that never ran gets
# counted as passing.
asyncio_mode = strict
testpaths = tests
